---
title: "GrecoStrom"
sidebarTitle: "GrecoStrom"
description: "Scenario A SST for grecostrom.gr: live GTM-N7G3WKL v103, sGTM v222, GA4 G-ZTF0MGDMMH. Inventory complete 8 Sep 2026."
icon: "bed"
---

<Warning>
  Multi-tenant domain. Do **not** create a new Stape or sGTM container. Do **not** publish sGTM Default Workspace **224** (live is **v222**). Hostname-scope every sGTM change. Cloudflare zone **is** available — same-origin `/metrics` is allowed after a Worker exists. Do **not** orange-cloud `sgtm.grecostrom.gr`. Keep TikTok pixel `C8B3LAFVFBF9K2CMOS6G`.
</Warning>

Scenario A record for [grecostrom.gr](https://grecostrom.gr/) (8 Sep 2026). Playbooks: [Server Side Tracking](/kb/web-analytics/server-side-tracking), [Cursor: existing Stape](/kb/web-analytics/cursor-prompt-existing-stape), [Customer GitHub repo](/kb/customer-github-repo).

Customer handbook repo: private `ht-grecostrom`. Never commit CAPI, Stape API keys, Cloudflare tokens, or Measurement Protocol secrets.

<Note>
  Live host is `ht-intra.mintlify.site`. Editor: [admin-mcp/grecostrom-customer](https://app.mintlify.com/hellenictechnologies/ht-intra/editor/admin-mcp%2Fgrecostrom-customer-50ccb34). Sheet ID `GTM-N7G3WK` is truncated — live public ID is **`GTM-N7G3WKL`**.
</Note>

## Current state (8 Sep 2026 — inventory)

| Piece | Live |
| --- | --- |
| Site | WooCommerce + WPML + GTM4WP 1.22.5 + Flying Press + **Cloudflare** (apex proxied, cache HIT). [shop.grecostrom.gr](https://shop.grecostrom.gr) still injects the same GTM. |
| Loader | Official `googletagmanager.com/gtm.js?id=GTM-N7G3WKL`. **Not** Custom Loader. Noscript is `data-lazy-src` (broken). `/metrics` **404**. |
| Web `GTM-N7G3WKL` | Account `6000107638`, container `30113322`, live **v103** (60 tags). `server_container_url` = `https://sgtm.grecostrom.gr/`. |
| sGTM | Shared Multi-tenant, live **v222** `starbucks tracking added` (112 tags). Default WS **224** is not live — fork live. |
| Tagging URL | [sgtm.grecostrom.gr/healthy](https://sgtm.grecostrom.gr/healthy) → `ok`. A `34.107.54.19` (DNS-only). |
| GA4 | Property `268233070` `GrecoStrom - GA4`, stream `G-ZTF0MGDMMH`. Linked to Ads `2980009656`. |
| Google Ads | CID `298-000-9656`. Conversion tracker `AW-594535532`. Purchase label `vEpACO6xhfUBEOzIv5sC`. |
| Meta | Ad account `821006281968800`. Pixel **`660914304818146`** GrecoStrom Main Pixel (events firing). |
| DV360 | Advertiser `8345751972` Grecostrom / Ringier `8218041317`. Floodlight advertiser **`15192785`** `invmedia` / `pagev0` + `sales0`. |
| TikTok | Web HTML pixel **`C8B3LAFVFBF9K2CMOS6G`**. **Not** in sGTM `LT - Tiktok ID`. |
| CMP | Cookiebot tags **paused**. Iubenda live in GTM (site `3446175`). No Cookiebot/iubenda in raw HTML except via GTM. |
| Cloudflare zone | `grecostrom.gr` `bbc61b9919149ae1b1b604405849fc9f` — token **can** manage this zone. |

## Hard locks

- Do **not** create a new Stape or sGTM container.
- sGTM: fork **live v222+**, never Default 224.
- Web: keep `GTM-N7G3WKL` only.
- `sgtm.` stays grey-cloud. Same-origin path `/metrics` only after Worker `X-From-Cdn: cf-stape`.
- Cookie Keeper is a **second** Generate (Stape User ID) after tags work.
- Keep TikTok. Do not delete DILIUSA / Px GD8386 pixels on the same Meta ad account (other brands).
- One Ads e-shop primary: **Purchase** `AW-594535532/vEpACO6xhfUBEOzIv5sC`. Store visits / Papafi local actions stay a **separate** goal.
- Web Ads conversion tags must not fire in parallel with sGTM Ads (Ads has no dedupe).
- Do not copy HT or Golden Gastro IDs.
- CAPI token stays in sGTM `LT - fb api token` only. Never git / this KB / chat.

## Public IDs

| System | Value |
| --- | --- |
| Site | [https://grecostrom.gr/](https://grecostrom.gr/) |
| Legacy shop | [https://shop.grecostrom.gr/](https://shop.grecostrom.gr/) |
| Web GTM | `GTM-N7G3WKL` (account `6000107638`, container `30113322`, live v103) |
| sGTM | Shared Multi-tenant account `6202173174`, container `169217628`, live v222 |
| sGTM host | `https://sgtm.grecostrom.gr` |
| GA4 | Account stream `G-ZTF0MGDMMH`, property `268233070` |
| Google Ads | CID `298-000-9656`, `AW-594535532` |
| Ads Purchase | `vEpACO6xhfUBEOzIv5sC` (primary today; keep) |
| Ads Add to cart | `c6zWCIXgkvUBEOzIv5sC` (observe) |
| Ads Checkout | `SSWGCJTvmaUZEOzIv5sC` (observe) |
| Meta ad account | `821006281968800` |
| Meta Pixel | `660914304818146` |
| Extra pixels on account | `527202269141241` DILIUSA, `579848138077239` Px GD8386 — do not delete |
| DV360 | Advertiser `8345751972` |
| Floodlight | Advertiser `15192785`, group `invmedia`, counter `pagev0`, sales `sales0` |
| TikTok | `C8B3LAFVFBF9K2CMOS6G` |
| iubenda | Site `3446175` (CS en `55513087` / el `76150047` / de `45322997`) |

## Keep / need / drop

**Keep**

- Funnel dataLayer: `view_item`, `add_to_cart`, `begin_checkout`, `purchase` (plus `view_cart` already tagged).
- Leads: contact form, phone click, employment form as **secondary** (`generate_lead` / `contact_form_submit`) — not Ads primary.
- Google Ads **Purchase** as the only e-shop primary (Enhanced Conversions). Store visits + local actions **separate**.
- Meta Pixel `660914304818146` hybrid + CAPI (sGTM `FB - *` via LT). Events already fire (PV/VC/ATC/IC/Purchase).
- DV360 Floodlight purchase `sales0` + page counter `pagev0` on advertiser `15192785`.
- TikTok pixel `C8B3LAFVFBF9K2CMOS6G`.
- Iubenda in web GTM. GTM4WP dataLayer (tags off / injection currently on — move to Custom Loader).
- sGTM LT rows already present for `grecostrom.gr` (GA4, Ads IDs/labels, FB pixel, Floodlight).
- Data Tag copies (`Copy of DT - *`) that feed sGTM.

**Need**

- Custom Loader from `sgtm.grecostrom.gr` (or `/metrics` after Worker). Enhanced ad-block protection.
- Fix web constant `CON - GA4 id` from placeholder `G-XXXXXXXXXX` to `G-ZTF0MGDMMH`.
- Cookie Keeper (second Generate, Stape User ID).
- Same-origin Worker `/metrics` (Cloudflare zone access confirmed).
- Pause/delete **web** `awct` Purchase (`SHOP - Purchase - Conversion - G.Ads`) so only sGTM Ads fires.
- Pause web `GT - Google Ads` if sGTM remarketing covers it.
- Server-only Floodlight: remove duplicate **client** Floodlight tags (`DC-15192785` and unused `DC-17252433` vars).
- TikTok Events API: add `grecostrom.gr` to `LT - Tiktok ID` / access token; keep web pixel + `event_id`.
- `ht_consent_ready` once per page; Iubenda purpose 2 granted (template currently denies **basic**).
- Raw `user_data` + stable `event_id` on conversions (Meta EMQ Excellent 8–10). Graph `can_proxy` is false.
- Cross-domain if ads still land on `shop.grecostrom.gr` (same GTM today; LT `contains grecostrom.gr` already matches).
- Funnel GA4/Ads/Meta audiences (ATC/checkout no purchase, purchasers 7/180, Lead 180d). Many Engagement Pack lists exist; cart-abandon GA4 lists are size 0.

**Drop** (GrecoStrom-owned only; after this table)

- Cookiebot CMP tags (already paused) — unused CMP.
- Paused leftover web `awct` (Contact Form, PDF, Website traffic, Telephone, Branches, SHOP Checkout/AddToCart).
- UA conversion actions still `include_in_conversions_metric=true` (Sessions > 5, Duration > 5 mins, Download PDF, old UA Purchase/Cart/etc.).
- Ads primaries that are not Purchase / store-visit: Website traffic, Contact Form, EmploymentFormSucess (`include=true`), Lead form, Clicks to call, local website visits as purchase-pot junk.
- Junk GA4 key events: `payment_success_page`, `initiate_checkout`, `cart___page_view`, `download_pdf`, Greek page-view names, `purchase___event`, `duration___5_mins`, `sessions___5`, etc. Keep `purchase`, `add_to_cart`, `begin_checkout`.
- Duplicate Floodlight pair (variable `DC-17252433` vs hardcoded `DC-15192785`) — keep **15192785** server-side only.
- Paused `BA - GA4 - Add to cart`, `Clickcease`, old `DT - *` (non-copy) blocked tags, `GA4 - Event - Ecommerce events`.
- Do **not** delete other-tenant sGTM tags/folders. Do **not** delete TikTok. Do **not** delete extra Meta pixels on the ad account.

## Timeline — start to finish

<Steps>
  <Step title="Phase 0 — Mintlify customer page (8 Sep 2026)">
    Created under Web Analytics → Customers.
  </Step>
  <Step title="Phase 1 — Inventory (8 Sep 2026)">
    Live site, web v103, sGTM v222, GA4, Ads, Meta, DV360, TikTok exported. Keep/need/drop written. No deletions yet.
  </Step>
  <Step title="Phase 2 — Housekeeping">
    Pending — delete unused GrecoStrom-owned items from the drop list.
  </Step>
  <Step title="Phase 3 — SST patch">
    Pending — Custom Loader, `/metrics` Worker, Cookie Keeper second Generate, GA4 ID fix, Ads server-only, Meta EMQ, TikTok Events API.
  </Step>
  <Step title="Phase 4 — Audiences">
    Pending — after events are clean.
  </Step>
  <Step title="Phase 5 — Verify">
    Pending — Stape checker vs before-PDF.
  </Step>
</Steps>

## Stape checker (before)

Source: [stape_report_before.pdf](https://drive.google.com/file/d/1S6CY5fmukdWUgqxwvzrcno7LYhnADF1X/view). Live HTML confirms the same gaps:

| Category | Live evidence |
| --- | --- |
| Cookies / ITP | Loader is third-party `googletagmanager.com`. No Cookie Keeper snippet. No `/metrics`. |
| Advertising + Analytics | GTM from googletagmanager.com; GA4 Google Tag uses placeholder `G-XXXXXXXXXX` plus `server_container_url`. Dual path likely. |
| Advertising | Web `awct` Purchase **live** plus sGTM `G-ADS - Purchase`. Dual Floodlight client+server. TikTok web-only. |
| Pagespeed | Official GTM + Floodlight googtag + TikTok events.js + iubenda CS from CDN. |

## Web GTM — live v103

Account `6000107638` / `GTM-N7G3WKL`.

Notable live tags:

- `GT - GA4 config` → `{{CON - GA4 id}}` = **`G-XXXXXXXXXX`** (broken constant). Config `server_container_url` = `https://sgtm.grecostrom.gr/`.
- `GA4 - ID` constant is already `G-ZTF0MGDMMH` (used by employment form only).
- `GT - Google Ads` `AW-594535532` live.
- `SHOP - Purchase - Conversion - G.Ads` **live** awct (dual with sGTM).
- iubenda template + `cHTML - iubenda multi-language` (site `3446175`). Cookiebot paused.
- Meta Pixel template tags live (`FB - PageView`, Copy of ViewContent/ATC/IC/Purchase) with `{{Unique Event Id}}`.
- TikTok HTML pixel live, consent `notNeeded`.
- Two Floodlight stacks live (client).
- Conversion Linker All Pages, consent `notNeeded`.

## sGTM — live v222

Generic `G-ADS - *`, `FB - *`, `GA4 - Base`, Floodlight Counter/Sales already hostname-routed. `grecostrom.gr` is **already** in:

- `LT - ga4 id` → `G-ZTF0MGDMMH`
- `LT - g-ads conversion id` → `594535532` + purchase/ATC/checkout/lead-style labels
- `LT - fb pixel id` → `660914304818146`
- `LT - fb api token` → present (do not copy)
- `LT - floodlight *` → `15192785` / `invmedia` / `pagev0` / `sales0`

**Missing LT:** `LT - Tiktok ID` has no `grecostrom.gr` row.

Do not delete GG / HT / Starbucks / other-tenant tags. Hostname-scope only if a leftover fires on this host.

## Meta

Pixel `660914304818146` fired in the last day (ViewContent, PageView, AddToCart, InitiateCheckout, Purchase). Custom conversions list empty. Website audiences exist (All Visitors, Cart 30d, Purchasers, Abandon Cart, 1% Purchase LAL). Missing vs plan: VC/ATC/IC/Purchase/Lead **180d** dedicated lists, value-based LAL confirmation, EMQ score (not invent — report from UI after SST).

## Google Ads conversions (housekeeping targets)

ENABLED junk primaries to strip from the purchase pot: Website traffic, Contact Form, EmploymentFormSucess (include=true), many local/YouTube. HIDDEN UA goals still `include_in_conversions_metric=true` — remove from conversions. Keep Purchase HT + Store visits as separate goals.

GA4-linked lists: `All Users of GrecoStrom - GA4`, `Purchasers of GrecoStrom - GA4` have size. Engagement Pack GA4 lists exist. Cart abandon `Shopping Stage: AddToCart | Abandoned | 30D` size **0**.

## Auth notes

- GA4/GTM: OAuth `digital@hellenictechnologies.com`.
- Ads: `google-ads-api` gRPC. Developer token env `GOOGLE_ADS_DEVELOPER_TOKEN`.
- Meta: `META_ACCESS_TOKEN` as HT-SysAdmin. CAPI only in sGTM LT.
- Cloudflare: zone-capable token sees `grecostrom.gr`.
- Stape REST not used this inventory (container keys present; do not log them).

## Work log

| Date | Phase | What |
| --- | --- | --- |
| 8 Sep 2026 | Page created | Stub under Web Analytics → Customers. |
| 8 Sep 2026 | Inventory | Live HTML + GTM web v103 + sGTM v222 + GA4 `G-ZTF0MGDMMH` + Ads conversions + Meta pixel `660914304818146` + TikTok `C8B3LAFVFBF9K2CMOS6G` + Floodlight `15192785`. Keep/need/drop filled. Cloudflare zone access yes. |

## Still open

| Item | Owner |
| --- | --- |
| Housekeeping from drop list | Agent |
| Custom Loader + `/metrics` Worker + Cookie Keeper | Agent / web-dev paste |
| Fix `CON - GA4 id` | Agent |
| TikTok Events API LT row | Agent |
| Meta EMQ Excellent | After user_data live |
| Stape re-scan vs before-PDF | After SST |
| Rotate CAPI if this session leaked the sGTM LT value | Human in Events Manager |

<Check>
  Inventory and keep/need/drop are on this page. Housekeeping can start. Do not rebuild containers.
</Check>
