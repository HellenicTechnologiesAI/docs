---
title: "GrecoStrom"
sidebarTitle: "GrecoStrom"
description: "Scenario A SST for grecostrom.gr: web v106, sGTM v223, /metrics Worker live. Custom Loader generated, not pasted."
icon: "bed"
---

<Warning>
  Multi-tenant domain. Do **not** create a new Stape or sGTM container. Do **not** publish sGTM Default Workspace **224** (live is **v223**). Hostname-scope every sGTM change. Cloudflare zone **is** available — same-origin `/metrics` Worker `gs-sgtm-metrics` is live. Do **not** orange-cloud `sgtm.grecostrom.gr`. Do **not** touch Worker `gg-sgtm-metrics`. Keep TikTok pixel `C8B3LAFVFBF9K2CMOS6G`. Cookie Keeper stays **off** while same-origin is on.
</Warning>

Scenario A record for [grecostrom.gr](https://grecostrom.gr/) (8 Sep 2026). Playbooks: [Server Side Tracking](/kb/web-analytics/server-side-tracking), [Cursor: existing Stape](/kb/web-analytics/cursor-prompt-existing-stape), [Customer GitHub repo](/kb/customer-github-repo).

Customer handbook repo: private `ht-grecostrom`. Never commit CAPI, Stape API keys, Cloudflare tokens, or Measurement Protocol secrets.

<Note>
  Live host is `ht-intra.mintlify.site`. Editor: [admin-mcp/grecostrom-customer](https://app.mintlify.com/hellenictechnologies/ht-intra/editor/admin-mcp%2Fgrecostrom-customer-50ccb34). Sheet ID `GTM-N7G3WK` is truncated — live public ID is **`GTM-N7G3WKL`**.
</Note>

## Current state (8 Sep 2026 — after housekeeping + SST)

| Piece | Live |
| --- | --- |
| Site | WooCommerce + WPML + GTM4WP 1.22.5 + Flying Press + **Cloudflare** (apex proxied). [shop.grecostrom.gr](https://shop.grecostrom.gr) still injects the same GTM. |
| Loader | Still official GTM4WP `googletagmanager.com/gtm.js?id=GTM-N7G3WKL` (URL-encoded). Noscript `data-lazy-src` (broken). Custom Loader **generated**, **not pasted**. |
| Web `GTM-N7G3WKL` | Account `6000107638`, container `30113322`, live **v106** `GS housekeeping Floodlight gone + ht_consent_ready pixels + EMQ keys` (33 tags). |
| sGTM | Shared Multi-tenant, live **v223** `GS SST Meta EMQ hostname-scope + TikTok Events API LT` (124 tags). Default WS **224** is not live — never publish it. |
| Same-origin | Worker **`gs-sgtm-metrics`**. Routes `grecostrom.gr/metrics*` + `www`. Cache bypass first. `X-From-Cdn: cf-stape`. |
| Proof | [grecostrom.gr/metrics/healthy](https://grecostrom.gr/metrics/healthy) → `ok`. `/metrics/gtm.js?id=GTM-N7G3WKL` 200. |
| Tagging URL | [sgtm.grecostrom.gr/healthy](https://sgtm.grecostrom.gr/healthy) → `ok`. A `34.107.54.19` (DNS-only). |
| GA4 | Property `268233070`, stream `G-ZTF0MGDMMH`. Key events: `purchase`, `add_to_cart`, `begin_checkout`. |
| Google Ads | CID `298-000-9656`. `AW-594535532`. Purchase `vEpACO6xhfUBEOzIv5sC`. |
| Meta | Ad account `821006281968800`. Pixel **`660914304818146`**. |
| DV360 | Advertiser `8345751972`. Floodlight **`15192785`** server-only after v106. |
| TikTok | Web pixel **`C8B3LAFVFBF9K2CMOS6G`** on `ht_consent_ready`. `LT - Tiktok ID` has `grecostrom.gr`. Access-token row **missing**. |
| CMP | Iubenda only (site `3446175`). Cookiebot **deleted**. |
| Cloudflare zone | `grecostrom.gr` `bbc61b9919149ae1b1b604405849fc9f`. |

## Hard locks

- Do **not** create a new Stape or sGTM container.
- sGTM: fork **live v223+**, never Default 224.
- Web: keep `GTM-N7G3WKL` only.
- `sgtm.` stays grey-cloud. Same-origin path `/metrics` via Worker `gs-sgtm-metrics`. Do **not** add a second Worker. Do **not** modify `gg-sgtm-metrics`.
- Cookie Keeper **off** in the snippet (same-origin). Do not Generate-with-Cookie-Keeper for this site.
- Keep TikTok. Do not delete DILIUSA / Px GD8386 pixels on the same Meta ad account (other brands).
- One Ads e-shop primary: **Purchase** `AW-594535532/vEpACO6xhfUBEOzIv5sC`. Store visits / Papafi local actions stay a **separate** goal.
- Web Ads conversion tags must not fire in parallel with sGTM Ads (Ads has no dedupe).
- Do not copy HT or Golden Gastro IDs.
- CAPI token stays in sGTM `LT - fb api token` only. Never git / this KB / chat.

## Public IDs

| System | Value |
| --- | --- |
| Site | [https://grecostrom.gr/](https://grecostrom.gr/) |
| Legacy shop | [https://shop.grecostrom.gr/](https://shop.grecostrom.gr/) |
| Web GTM | `GTM-N7G3WKL` (account `6000107638`, container `30113322`, live **v106**) |
| sGTM | Shared Multi-tenant account `6202173174`, container `169217628`, live **v223** |
| sGTM host | `https://sgtm.grecostrom.gr` |
| Same-origin | `https://grecostrom.gr/metrics` (Worker `gs-sgtm-metrics`) |
| GA4 | Account stream `G-ZTF0MGDMMH`, property `268233070` |
| Google Ads | CID `298-000-9656`, `AW-594535532` |
| Ads Purchase | `vEpACO6xhfUBEOzIv5sC` (primary today; keep) |
| Ads Add to cart | `c6zWCIXgkvUBEOzIv5sC` (observe) |
| Ads Checkout | `SSWGCJTvmaUZEOzIv5sC` (observe) |
| Meta ad account | `821006281968800` |
| Meta Pixel | `660914304818146` |
| Extra pixels on account | `527202269141241` DILIUSA, `579848138077239` Px GD8386 — do not delete |
| DV360 | Advertiser `8345751972` |
| Floodlight | Advertiser `15192785`, group `invmedia`, counter `pagev0`, sales `sales0` |
| TikTok | `C8B3LAFVFBF9K2CMOS6G` |
| iubenda | Site `3446175` (CS en `55513087` / el `76150047` / de `45322997`) |

## Keep / need / drop

**Keep**

- Funnel dataLayer: `view_item`, `add_to_cart`, `begin_checkout`, `purchase` (plus `view_cart` already tagged).
- Leads: contact form, phone click, employment form as **secondary** (`generate_lead` / `contact_form_submit`) — not Ads primary.
- Google Ads **Purchase** as the only e-shop primary (Enhanced Conversions). Store visits + local actions **separate**.
- Meta Pixel `660914304818146` hybrid + CAPI (sGTM `FB - *` via LT). Events already fire (PV/VC/ATC/IC/Purchase).
- DV360 Floodlight purchase `sales0` + page counter `pagev0` on advertiser `15192785`.
- TikTok pixel `C8B3LAFVFBF9K2CMOS6G`.
- Iubenda in web GTM. GTM4WP dataLayer (tags off / injection currently on — move to Custom Loader).
- sGTM LT rows already present for `grecostrom.gr` (GA4, Ads IDs/labels, FB pixel, Floodlight).
- Data Tag copies (`Copy of DT - *`) that feed sGTM.

**Need** (still open after SST patch)

- Web-dev **REPLACE** GTM4WP official snippet with Stape Generate (Cookie Keeper **off**, Same Origin Path `/metrics`). Turn GTM4WP container injection **off**. Purge Flying Press + Cloudflare HTML. Noscript `https://sgtm.grecostrom.gr/ns.html?id=GTM-N7G3WKL`.
- Human Ads UI: uncheck Included in Conversions on leftover UA HIDDEN goals. `EmploymentFormSucess - HT` is still include+primary (API cannot clear it).
- Human: `grecostrom.gr` row on `LT - Tiktok Access Token` with a **pixel-specific** token for `C8B3LAFVFBF9K2CMOS6G`. Do **not** reuse the HT token. Events API tags are already live.
- Human Meta: VC/ATC/IC/Purchase/Lead **180d** WCAs on pixel `660914304818146` (Graph cannot create ads). Do not attach to live spend until membership looks real.
- Stape re-scan after loader paste. EMQ Excellent 8–10 after real orders with email/phone.

**Drop** (GrecoStrom-owned; done unless noted)

- Cookiebot CMP tags — **deleted**.
- Paused leftover web `awct` — **deleted**.
- Client Floodlight `DC-15192785` + unused `DC-17252433` vars — **deleted** (v106). Keep **15192785** server-side.
- Junk GA4 key events — **deleted**. Keep `purchase`, `add_to_cart`, `begin_checkout`.
- Web Ads Purchase + `GT - Google Ads` — **paused** (sGTM sends Ads; Ads has no dedupe).
- UA conversion include flags — **not writable via API**. Human Ads UI.
- Do **not** delete other-tenant sGTM tags/folders. Do **not** delete TikTok. Do **not** delete extra Meta pixels on the ad account.

## Timeline — start to finish

<Steps>
  <Step title="Phase 0 — Mintlify customer page (8 Sep 2026)">
    Created under Web Analytics → Customers.
  </Step>
  <Step title="Phase 1 — Inventory (8 Sep 2026)">
    Live site, web v103, sGTM v222, GA4, Ads, Meta, DV360, TikTok exported. Keep/need/drop written. No deletions yet.
  </Step>
  <Step title="Phase 2 — Housekeeping (8 Sep 2026)">
    Web **v104** then **v106**: deleted Cookiebot, leftover awct, Clickcease, BA GA4, generic ecommerce, old DT/FB copies, Floodlight `DC-17252433` stack, and **client** Floodlight `DC-15192785` (tags 250/251/252 + vars 253–256). Paused `SHOP - Purchase - Conversion - G.Ads` (40) and `GT - Google Ads` (206). GA4 junk key events deleted. Ads API demoted ENABLED webpage junk; UA HIDDEN includes left for the Ads UI.
  </Step>
  <Step title="Phase 3 — SST patch (8 Sep 2026)">
    Cloudflare Worker `gs-sgtm-metrics` + `/metrics` cache bypass. Web **v105** then **v106**: `CON - GA4 id` = `G-ZTF0MGDMMH`, transport `https://grecostrom.gr/metrics`, iubenda purposeBasic granted, `GS | ht_consent_ready` on 241+242, base Meta/TikTok/DT PageView on `CE - ht_consent_ready` (262). DT user_data city/country/zip/`user_id`. FB AM zip/country/`external_id`.

    sGTM **v223** (fork live v222, **not** Default 224): `grecostrom.gr` → `C8B3LAFVFBF9K2CMOS6G` on `LT - Tiktok ID`. `GS | SST Meta *` (billing ED keys only — no IP geo, Event Enhancement, HttpOnly, `adStorageConsent` required). Shared `FB - PageView/VC/ATC/IC/Purchase/Lead` **blocked** on `grecostrom.gr`. `GS | SST TikTok *` hostname-scoped. Access-token row **not** added (do not reuse HT token). Custom Loader generated; site still official GTM4WP.
  </Step>
  <Step title="Phase 4 — Audiences (8 Sep 2026)">
    GA4 `GS | *` lists created (ads export on): purchasers 7/180, ATC/checkout/view_item no purchase, leads 90d. Meta 180d WCAs **not** created (Graph ads permission). Existing Meta visitors/cart/purchasers/1% Purchase LAL kept. Not attached to live spend.
  </Step>
  <Step title="Phase 5 — Verify">
    `/metrics` healthy 200. Live versions v106 / v223. Homepage still official GTM4WP — Stape checker will not leave Improve until the loader paste. EMQ / Test Events after a real consented purchase.
  </Step>
</Steps>

## Stape checker (before vs now)

Source: [stape_report_before.pdf](https://drive.google.com/file/d/1S6CY5fmukdWUgqxwvzrcno7LYhnADF1X/view).

| Category | Before | Now (8 Sep 2026) |
| --- | --- | --- |
| Cookies / ITP | Third-party `googletagmanager.com`. No `/metrics`. | Worker + `/metrics` **live**. Site snippet **still** official GTM. Cookie Keeper **off** (correct with same-origin). |
| Advertising + Analytics | GTM from Google; GA4 placeholder `G-XXXXXXXXXX`. | GA4 id + transport `/metrics` in GTM. Library still loads from Google until Custom Loader paste. |
| Advertising | Web awct Purchase + sGTM Ads. Client Floodlight. TikTok web-only. | Web Ads **paused**. Floodlight **server-only**. TikTok Events API tags live, token row **missing**. |
| Pagespeed | Official GTM + Floodlight gtag + TikTok events.js. | Client Floodlight gtag **gone**. Official GTM + TikTok pixel remain until loader paste. |

Re-run the checker **after** web-dev pastes Generate and purges HTML cache. Do not invent a new loader filename if Stape still shows Custom Loader +4 after a byte-identical paste.

## Custom Loader — web-dev paste (Cookie Keeper off)

Generate (8 Sep 2026): Domain `sgtm.grecostrom.gr`, Web GTM `GTM-N7G3WKL`, Enhanced ad blocker **ON**, Same Origin Path **`/metrics`**, Platform **Other**, Cookie Keeper **off**, original GTM code **off**.

The loader **filename is shared** on this Multi-tenant Stape container. The **query** selects GrecoStrom. Do **not** invent a different file. Do **not** copy the HT query. Bare file without query = 404.

**REPLACE** the GTM4WP head IIFE. Turn GTM4WP **container** injection off. Keep GTM4WP dataLayer / Woo ecommerce JS. Paste from Stape Generate (byte-identical, including official HTML comments). Confirmed 200 via `/metrics/{file}.js?{query}` (~624KB).

Noscript (not same-origin):

```html
<!-- Google Tag Manager (noscript) -->
<iframe src="https://sgtm.grecostrom.gr/ns.html?id=GTM-N7G3WKL" height="0" width="0" style="display:none;visibility:hidden"></iframe>
<!-- End Google Tag Manager (noscript) -->
```

No `title`. No `data-lazy-src`. Apply on `grecostrom.gr` and `shop.grecostrom.gr` if ads still land there. Purge Flying Press + Cloudflare HTML.

## Web GTM — live v106

Account `6000107638` / `GTM-N7G3WKL`.

- iubenda Community Gallery, purposeBasic **granted**, ads/measurement denied until accept.
- `GS | ht_consent_ready` (261) on Initialization All Pages [MKT] (241) **and** `CE - consent_update` [MKT] (242), once per page.
- `CE - ht_consent_ready` (262): `FB - PageView`, TikTok Pixel (consent needed), `DT - PageView`.
- Conversion Meta / DT still on `[MKT]` ecommerce triggers.
- `CON - GA4 id` = `G-ZTF0MGDMMH`. `CON - transport url` = `https://grecostrom.gr/metrics`.
- **Paused:** tag 40 web Purchase awct, tag 206 `GT - Google Ads`.
- **Deleted vs v103:** Cookiebot, leftover awct, Clickcease, client Floodlight, unused DC-17252433 vars, junk copies.
- TikTok kept. Data Tag copies kept (`Copy of DT - *`).

## sGTM — live v223

Generic `G-ADS - *`, `GA4 - Base`, Floodlight Counter/Sales still hostname-routed via LT. `grecostrom.gr` remains in GA4 / Ads / FB pixel / FB token / Floodlight LTs.

**New in v223**

- `LT - Tiktok ID`: `grecostrom.gr` → `C8B3LAFVFBF9K2CMOS6G`.
- `GS | SST Meta PageView / ViewContent / AddToCart / InitiateCheckout / Purchase / Lead` — Data Client + hostname contains `grecostrom.gr`. User data from ED billing fields + `external_id`. **No** IP geo hashed fields. Event Enhancement on. HttpOnly cookies on. `adStorageConsent` required.
- `GS | Block leftover on grecostrom.gr` on shared `FB - PageView / VC / ATC / IC / Purchase / Lead`.
- `GS | SST TikTok *` Pageview / ViewContent / AddToCart / InitiateCheckout / CompletePayment / SubmitForm. Token from `LT - Tiktok Access Token` (**empty for this host until a pixel-specific token is added**).

Do not delete GG / HT / Starbucks / other-tenant tags.

**Missing LT:** `LT - Tiktok Access Token` still has no `grecostrom.gr` row.

## Meta

Pixel `660914304818146` is the only GrecoStrom pixel. Custom conversions list empty. Existing website audiences (**keep**): All Visitors 7/30/60/90/180d, Cart 30 Days, Purchasers 14d / 30D HT, PageView 30d, 1% Purchase LAL. `Abandon Cart - 30 Days` has `pixel_id` 0 — wrongly sourced; do not attach to spend; do not delete until campaigns are checked.

**Not created (Graph ads permission):** `GS | ViewContent/AddToCart/InitiateCheckout/Purchase/Lead 180d`. Human in Events Manager. Do not attach new lists to live spend until membership looks real. Do not mint another lookalike on an empty new seed.

EMQ: not invented. After a consented purchase with email/phone, read Events Manager. Target Excellent 8–10 on Purchase / IC / ATC / VC / Lead. CAPI now uses billing ED keys (no IP geo) + Event Enhancement on `GS | SST Meta *`.

## Audiences (8 Sep 2026)

**GA4 property `268233070`** (ads personalization **on** — will sync to Ads; do not attach to live campaigns yet):

| Audience | ID |
| --- | --- |
| GS \| Purchasers 7D | `15739076611` |
| GS \| Purchasers 180D | `15739103068` |
| GS \| ATC no purchase 7D | `15739033262` |
| GS \| ATC no purchase 30D | `15739009641` |
| GS \| Checkout no purchase 7D | `15739121851` |
| GS \| Checkout no purchase 30D | `15739059604` |
| GS \| View item no purchase 30D | `15739116213` |
| GS \| Leads 90D | `15739114919` |

Already present (kept): `Purchasers` 540d, `Purchasers 30D | HT`, Engagement Pack lists, `Users * | HT`. Ads `Shopping Stage: AddToCart | Abandoned | 30D` size **0** — use the new GA4 ATC-no-purchase lists once they fill. Mattress/bed/baby view_item splits skipped until category volume is confirmed.

**Google Ads:** `All Users of GrecoStrom - GA4` / `Purchasers of GrecoStrom - GA4` have size. New `GS | *` lists appear as GA4 remarketing after sync. Use as PMax **signals** and purchaser **exclusions** only after membership is non-zero. Papafi store-visit stays separate.

**TikTok:** no website audiences created (Events API token not on this hostname yet).

## Google Ads conversions

Purchase HT `513890542` kept INCLUDE+PRIMARY. ENABLED webpage junk demoted where the API allowed. **Human Ads UI:** uncheck Included in Conversions on leftover UA HIDDEN goals (Sessions > 5, Duration > 5 mins, old UA Purchase/Cart, etc.). Keep Store visits as a **separate** goal. `EmploymentFormSucess - HT` is still include+primary (API cannot clear it).

## Auth notes

- GA4/GTM: OAuth `digital@hellenictechnologies.com`.
- Ads: `google-ads-api` gRPC. Developer token env `GOOGLE_ADS_DEVELOPER_TOKEN`.
- Meta: `META_ACCESS_TOKEN` as HT-SysAdmin. CAPI only in sGTM LT.
- Cloudflare: zone-capable token sees `grecostrom.gr`.
- Stape Account API from this VM **did** Generate this time (`api.app.stape.io`). Cookie Keeper left **off**.
- Env `TIKTOK_EVENTS_API_TOKEN` matches **HT** `HT TikTok Access Token` — **not** used on GrecoStrom.

## Work log

| Date | Phase | What |
| --- | --- | --- |
| 8 Sep 2026 | Page created | Stub under Web Analytics → Customers. |
| 8 Sep 2026 | Inventory | Live HTML + GTM web v103 + sGTM v222 + GA4 `G-ZTF0MGDMMH` + Ads + Meta pixel `660914304818146` + TikTok + Floodlight `15192785`. Keep/need/drop filled. |
| 8 Sep 2026 | Housekeeping | Web v104/v106 deletions (Cookiebot, leftover awct, client Floodlight, junk). GA4 key events cleaned. Ads API demote where writable. |
| 8 Sep 2026 | SST | Worker `/metrics`. Web v105/v106 consent + EMQ keys. sGTM v223 GS Meta + TikTok LT/tags. Custom Loader generated, not on site. |
| 8 Sep 2026 | Audiences | Eight GA4 \`GS | \*\` lists. Meta 180d WCAs blocked on permission. |

## Still open

| Item | Owner |
| --- | --- |
| Paste Custom Loader + noscript; GTM4WP container off; purge caches | Web-dev |
| Ads UI: uncheck Included in Conversions on UA leftovers; EmploymentFormSucess include | Human in Ads |
| `LT - Tiktok Access Token` row for `grecostrom.gr` (pixel-specific) | Human / Stape UI |
| Meta 180d VC/ATC/IC/Purchase/Lead WCAs | Human in Ads Manager |
| Stape re-scan vs before-PDF | After loader paste |
| Meta EMQ Excellent + Test Events (do not publish Test ID) | After real order |
| Rotate CAPI if inventory leaked `LT - fb api token` | Human in Events Manager |
| Category-split view_item audiences (mattresses/beds/baby) | After volume check |

<Check>
  Housekeeping and the GTM/sGTM SST patch are live (web v106 / sGTM v223 / `/metrics` Worker). The site still loads official GTM until web-dev pastes Generate. Do not rebuild containers. Do not publish Default 224. Do not enable Cookie Keeper on this same-origin snippet.
</Check>
