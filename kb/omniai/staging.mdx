---
title: "OmniAI staging"
sidebarTitle: "Staging"
description: "Staging is up at staging.omniaichatbot.com. Local Postgres and Redis. Do not point it at live data."
icon: "flask"
---

Status on **1 September 2026 (afternoon)**: staging is **up** and behind **Cloudflare Access**. Open [https://staging.omniaichatbot.com](https://staging.omniaichatbot.com). You get a Cloudflare PIN login first. Allowed emails only: `abedin@hellenictechnologies.com`, `michelidis@hellenictechnologies.com`, `web@hellenictechnologies.com`. Then log in to OmniAI the same way as live.

<Warning>
  Do not change Cloudflare `app` or `hc`. Do not point staging at the live managed clusters you see in the DigitalOcean project (`omniai-prod-pg`, `omniai-prod-valkey`). Those are live only. Staging mail fetch from live mailboxes is off so it cannot pull or send live mail.
</Warning>

| Field | Value |
| --- | --- |
| Name | `omniai-staging-ams3` |
| Public IP | `164.92.220.232` |
| Private IP | `10.50.0.5` |
| Droplet ID | `596861262` |
| Size | same as live: 8 GB / 4 vCPU / 160 GB, AMS3 |
| SSH | `ssh -i ~/.ssh/omniai_do root@164.92.220.232` |
| Cost | about 48 USD / month extra. Live managed DB bill stays unchanged |

What is running: Caddy to `127.0.0.1:3000`; `omniai-rails-1` and `omniai-sidekiq-1` on `chatwoot:production`; local Postgres (`pgvector/pgvector:pg17`, compose host `postgres`) with a read-only copy of live data; local Redis 7. `FRONTEND_URL` is `https://staging.omniaichatbot.com`. Promote script: `/opt/omniai/scripts/promote-staging-to-live.sh` (`DRY_RUN=1` copies the image only).

## How to use it

How to ship: [Staging to live](/kb/omniai/deploy-hotfixes). Copy files here first, check, then the same files on live. Restart, never recreate. Never compile on live. Never point staging at the live managed database.

Those bootstrap steps are **already done**. Do not install a second database or dump live again unless a human asks to refresh the copy.
