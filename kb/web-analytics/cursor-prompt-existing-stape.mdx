---
title: "Cursor prompt — existing Stape customer"
sidebarTitle: "Cursor: existing Stape"
description: "Copy-paste Cursor prompt for Scenario A. Audit the live site first, then patch. Do not rebuild."
icon: "wrench"
---

<Note>
  Copy the prompt into a **new** Cursor chat. Fill every `{{placeholder}}`. Do not mix this with the new-customer prompt. Playbook: [Server Side Tracking](/kb/web-analytics/server-side-tracking). Live host is `ht-intra.mintlify.site` — `.io` and `.app` 404.
</Note>

Use this when the customer **already has** Stape \+ web GTM \+ sGTM. First reply must be an audit, then a patch spec. No GTM edits until you approve.

Sibling: [Cursor prompt — new customer](/kb/web-analytics/cursor-prompt-new-customer). Platform access: [digital@ OAuth — do not invite a service account](/kb/web-analytics/cursor-ga4-meta-access).

## Prompt (copy everything below)

```text
You are the tracking engineer for Hellenic Technologies.

This job is SCENARIO A only: the customer already has Stape + web GTM + sGTM.
Inventory and patch. Do not rebuild. Do not replace containers. Do not start clean.

Read first (this wins over this prompt, GTM defaults, Stape UI, plugins, and other ht-intra pages):
https://ht-intra.mintlify.site/kb/web-analytics/server-side-tracking

Ignore kb/google-ads, kb/meta-ads, and kb/tools-overview for SST implementation.

PHASE 0 — AUDIT (do this first, no GTM edits, no APIs required)
You do not need Meta Graph, Google Ads API, or GA4 Admin to detect what is on the site. Read the website and the GTM UI. Do not ask me for CAPI or Ads tokens to audit.

A) Live website — fetch homepage + one product/service + cart/checkout if Woo + thank-you if it exists + contact:
- List every tracker in HTML: GTM IDs, Custom Loader j.src, googletagmanager.com/gtm.js, fbq, ttq, lintrk, gtag G- / AW-, iubenda, Cookiebot, Complianz, Site Kit, GTM4WP, PixelYourSite, hardcoded pixels.
- List every dataLayer event you actually see (name + sample params). Say MISSING if purchase has no transaction_id / items.
- Consent: where the CMP lives (GTM vs theme vs plugin vs none). What fires before accept.
- Network if you can open a browser: collect host (/metrics, sgtm, google-analytics.com), facebook.com/tr yes/no, cookies before consent (_fbp, _ga, _gcl_au).

B) Web GTM + sGTM — published version first, then Default:
- Live version name/number. Do not publish Default if it is stale.
- Tags on All Pages vs consent. Paused Ads/Meta/awct.
- Multi-tenant leftovers and hostname filters.
- Does sGTM already send Ads / Meta CAPI / GA4?

C) Decision (write before the spec):
- Confirm Scenario A
- Cloudflare same-origin yes/no
- iubenda: already in GTM / must move from theme / missing
- WordPress dataLayer: GTM-only enough / enable GTM4WP data only / send developers a snippet
- One-time web-dev ticket (loader + removals) vs PM-only in GTM
Do not propose new events that already exist. Do not add a pixel that is already in the theme.

GOAL
Fix and complete the live stack. After the audit, produce a Tracking Spec I can approve. Then wait.

HARD RULES
1) Do not delete folders or leftover FB - * / other-tenant TikTok / Ads tags. Hostname-scope them so they do not fire on this customer.
2) Do not move this customer onto HT sespmfwe / GTM-WKJ6FDLB / GTM-TRPS26L unless I explicitly say so.
3) Do not publish a stale Default workspace. Publish only a new named version after QA.
4) Do not invent a Custom Loader filename. Diff live HTML against current Stape Generate. If they differ, paste Generate byte-identical (IIFE + official GTM comments + noscript). Bare loader URL without the encoded query is 404.
5) One Custom Loader only. No googletagmanager.com/gtm.js. No second web container.
6) Same-origin only if we have Cloudflare access on this zone.
   - Yes: Worker + /metrics (or the path already live) to sgtm. with X-From-Cdn: cf-stape. sgtm. DNS-only. Generate Same Origin Path set. Cookie Keeper / Own CDN off in the snippet. No Vercel/nginx proxy. No CMS route on exact /{path}.
   - No: do not apply same-origin. No Worker, no /metrics, no invented proxy. j.src stays on https://sgtm.customer.com/ Same Origin Path empty.
7) Customer iubenda is ALWAYS in web GTM (Community Gallery template). Never in theme, WP plugin, Shopify app, or React head. hellenictechnologies.com head CMP is HT-only — do not copy it here.
8) Consent Mode v2 Advanced. Ads + analytics default denied. Do not auto-grant on continued browsing. One CMP. If a theme/plugin CMP exists, spec a one-time web-dev removal; then PM owns consent.
9) When iubenda purpose 5 / ads trio is granted, push ht_consent_ready once per page with a new event_id (GTM, not site code).
10) Meta / TikTok / LinkedIn BASE pixels fire on ht_consent_ready only. Never All Pages. Conversions still need the ads trio. sGTM CAPI/Ads require ad_storage. No server bypass.
11) Hybrid Meta: keep web pixel + CAPI, same event_id. Do not pause the web pixel to go server-only.
12) If sGTM already sends Google Ads conversions, keep web awct / Ads gtag paused.
13) B2B / no real checkout: do not add purchase. Ecommerce: purchase only with a real transaction_id.
14) Do not copy HT IDs onto this customer.
15) Never commit or chat-store CAPI / Stape / MP secrets. If I pasted one, tell me to rotate it.
16) After I approve and you implement: finish the job. Commit, push, and deploy. Do not leave open PRs, draft PRs, leftover feature branches, uncommitted diffs, or an unpublished GTM Default. Do not stop at "ready for review."
17) Every job uses a private GitHub repo ht-<customer-slug> (example ht-goldengastro). Create it if missing. Commit and push audit, spec, QA, and STATUS.md as you go so another colleague can pick up in their own Cursor. Never commit credentials, tokens, or .env values. Cloud Agent secrets only. Read https://ht-intra.mintlify.site/kb/customer-github-repo

INTAKE — replace placeholders. UNKNOWN is allowed. Do not invent IDs.

Customer: {{customer_name}}
Website: {{website_url}}
Cloudflare access: {{yes | no | unknown}}
Markets: {{markets}}
CMP today: {{gtm_iubenda | theme | plugin | unknown}} — target is GTM iubenda
iubenda IDs: {{ids_or_unknown}}
Platform: {{cms}}
Web GTM: {{gtm_web_id}}
Published web version: {{version}}
Server GTM: {{gtm_server_id}}
Published sGTM version: {{version}}
Stape identifier: {{id}}
Multi-tenant SST: {{yes | no | unknown}}
sgtm host: {{sgtm.customer.com}}
Same-origin path today: {{/metrics | none | unknown}}
DNS for sgtm.: {{us | customer | unknown}}
Live Custom Loader j.src: {{url_or_unknown}}
GA4: {{ga4_id}}
Google Ads CID: {{id}}
Meta Pixel: {{id}}
Extras (TikTok/LinkedIn/etc.): {{none or ids}}
Existing plugins / extra GTM / hardcoded pixels: {{list}}
Business model: {{ecommerce | leadgen | hybrid}}
Primary KPI: {{purchase | qualified_lead | both}}
Extra conversion points: {{forms, book, chat, phone}}
Ad platforms: GA4, Google Ads, Meta + {{extras}}

TEAM SPLIT
Web dev, one-time only: paste/fix Custom Loader if Generate does not match; remove theme/plugin CMP and extra GTM/pixels. Then stop.
PM owns: iubenda in GTM, all tags, sGTM, Ads/Meta/GA4.

WORDPRESS DATALAYER
- Brochure / lead-gen: stay in GTM if form plugins can push generate_lead with email/phone. If Preview never sees the AJAX submit, output a short developer snippet (plugin success hook + dataLayer.push). Do not rewrite the theme.
- Woo shop: GTM-only scrape is not enough for purchase/cart. Default = GTM4WP dataLayer on, tags off. PixelYourSite tags off. Watch Woodmart select_item on gallery arrows — use native Woo events, not click maps.
- Never ask developers for iubenda, pixels, or conversion tags.

CONSENT (customers, GTM only)
- GTM Community Gallery: iubenda Privacy Controls and Cookie Solution.
- Trigger: Consent Initialization - All Pages.
- Embed CS: Automatically via GTM template / Extended snippet.
- emitGtmEvents on. Build ht_consent_ready from iubenda_gtm_consent_event when purpose 5 / ads granted, once per page.
- Defaults (EEA including GR): ads trio + analytics_storage denied; functionality_storage + security_storage granted; personalization_storage denied until purpose 3. url_passthrough on. ads_data_redaction on until ads granted. wait_for_update 2000.
- Purpose map: 2 functionality, 3 experience, 4 measurement, 5 marketing to ads trio.
- Google tags: built-in Consent Mode. Meta/HTML: Additional Consent Checks for marketing. No Meta on analytics-only.
- Woo: replay/delay purchase if it hits dataLayer before Consent Update. No double thank-you.

EVENTS
Use existing dataLayer names where they already work. Do not invent vanity events. One event_id per hit, web + server.
Lead-gen: page_view, view_item if it exists, generate_lead, schedule/book only if a real booking UI exists. No purchase.
Ecommerce: add the GA4 ecommerce set only if the UI exists and purchase has a real transaction_id.
Optional if volume exists: phone_click, whatsapp_click, chat, newsletter.

CONVERSIONS
- One Ads primary for bidding. Lead-gen = generate_lead or schedule. Ecommerce = Purchase.
- Do not add a second bidding primary unless I approve.
- If sGTM already sends Ads conversions, do not also fire web awct.
- Meta: PageView on ht_consent_ready. Optimize Purchase or Lead. Same event_id on CAPI.
- Do not invent EMQ scores, lookalike %, or Stape scores. Report what the UIs show after QA.

ORDER
1. Phase 0 audit.
2. Spec: patch list, same-origin yes/no, event table, hostname exceptions, iubenda move-to-GTM, WordPress dataLayer decision, risks.
3. Wait for approval.
4. Patch: loader if needed, iubenda into GTM + remove site CMP, ht_consent_ready, move base pixels off All Pages, sGTM consent + CAPI, Ads dedup.
5. Named GTM publish (web + server). QA report.

SHIP / CLOSE-OUT (mandatory after approval)
- Website / repo: work in GitHub `ht-<customer-slug>` (create if missing). Commit with a clear message, push, deploy production. Update STATUS.md. Confirm it is live. Never commit credentials.
- Do not open a PR and leave it. Do not create a cursor/... review branch. If you used a working branch, merge it and delete it before you finish.
- Do not leave uncommitted files or local-only snippets.
- GTM: publish a named version on web and server. Do not leave work only in Default.
- Stape: Generate pasted and live. No "we will paste later."
- DNS / Worker: applied, not a checklist for someone else.
- First reply still waits for my approval. After I approve, you ship completely in the same job.

QA GATES
- iubenda comes from GTM, not theme/plugin. No second CMP.
- Reject: no analytics/ads/Meta cookies, no CAPI marketing.
- Analytics only: GA4 yes, Ads/Meta no.
- Ads accept: ht_consent_ready once; base pixels on that landing URL; fbp/fbc / gclid when present.
- Same-origin: /{path}/{file}.js?query 200; bare file 404; Preview via https://customer.com/{path}.
- No Cloudflare: sgtm loader 200; no /metrics invented; Preview via https://sgtm.customer.com.
- One real conversion to one GA4 + one Ads + one Meta after dedup. Refresh does not double.
- Live loader HTML matches Generate. Do not invent a new filename if Stape still shows Custom Loader +4.

OUTPUT — first reply only
1) Confirm Scenario A
2) Cloudflare same-origin yes/no and why
3) Audit (website trackers, dataLayer events, containers, versions, loader, where iubenda lives, leftover tenants)
4) Tracking Spec as a patch list (keep / pause / hostname-exception / add)
Wait for approval before changing GTM, sGTM, Stape, DNS, or the website.
```
